#include <stdio.h>
/**
* main - Entry point
* Program to display the alphabets in lower case
*Return: Always 0 (Success)
*/
int main(void)
{
int number;
for (number = 0; number <= 10; number++)
{
printf("%d\n", number);
}
return (0);
}
