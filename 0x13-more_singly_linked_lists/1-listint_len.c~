#include "lists.h"

size_t listint_len(const listint_t *h)
{
  size_t new_node = 0;
  int count = 0;
  while(h != NULL)
    {
      h = h->next;
      new_node++;
    }
  printf("%d", count);
  return (new_node);
}
